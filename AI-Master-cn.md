# 九连棋 AI 引擎设计规范

## 1. 概述

本项目实现了一个具备完整博弈能力的九连棋（Nine Men's Morris）AI 引擎。引擎采用 Minimax + Alpha-Beta 搜索，配合阶段感知的静态评估函数和动态策略状态机，支持三种性能模式（Eco / Normal / Master），可生成结构化战术报告以对接 LLM 自然语言输出。

## 2. 系统架构

采用"感知-决策-表达"三层分离架构：

### 2.1 引擎层 (engine.js)

负责棋盘状态维护和规则执行：

- **棋盘表示**：24 位数组，每个位置存储玩家类型或 null
- **玩家状态**：每方维护 `piecesOnHand`（待放置）、`piecesOnBoard`（盘面存活）、`piecesLost`（已被吃）
- **走法生成**：`generateLegalMoves(player)` 根据当前阶段（放置/移动/飞行）生成所有合法走法
- **走法执行**：`makeMove(move)` 执行走法，返回 `{ formedMill, move }`，成行时设置 `millMove` 状态但不切换玩家
- **走法撤销**：`undoMove()` 精确还原棋盘和玩家状态，支持 AI 深度搜索
- **序列化**：FEN 格式（`MILL-FEN`）支持棋盘状态的存储和恢复
- **成行检测**：`isInMill(board, pos, player)` 检查指定位置是否属于某个磨坊

### 2.2 战术分析层 (strategy.js)

将棋盘坐标转化为结构化战术数据：

- **阵型张力分析**：`analyzeFormationTension(player)` 返回威胁数、叉子数等指标
- **机动性计算**：`countMobility(player)` 统计安全移动数
- **战术报告**：`generateReport()` 输出标准化 JSON，包含阶段、材料差、机动性差、建议走法及标签

### 2.3 AI 控制层 (ai.js)

搜索、评估、决策的核心模块：

- **Minimax 搜索**：带 Alpha-Beta 剪枝的博弈树搜索
- **静态评估**：多因子加权评估函数
- **策略状态机**：根据局势动态切换博弈性格
- **性能模式**：三档难度配置
- **走法选择**：基于排名的指数分布随机选择

## 3. 九子棋规则引擎要点

### 3.1 三阶段流转

| 阶段 | 条件 | 走法类型 |
|------|------|---------|
| PLACEMENT | `piecesOnHand > 0` | `place`：从手牌放到空位 |
| MOVING | 双方 `piecesOnHand === 0`，且 `piecesOnBoard > 3` | `move`：移动到相邻空位 |
| FLYING | `piecesOnBoard <= 3` 且 `piecesOnHand === 0` | `fly`：移动到任意空位 |

### 3.2 成行与吃子的双 Ply 结构

九子棋的"成行并吃子"在引擎中拆分为两个独立 ply：

1. **走子成行**：`makeMove` 返回 `formedMill: true`，设置 `state.millMove = true`，**不切换玩家**
2. **选择吃子**：生成 `type: 'remove'` 的走法，执行后重置 `millMove = false`，切换玩家

这个双 ply 结构是后续"半回合水平线效应"修复的基础。

### 3.3 吃子合法性

吃子时必须遵守：已被包围的磨坊中的棋子不能被吃（除非所有棋子都在磨坊中）。引擎在 `generateLegalMoves` 中通过 `isInMill` 和邻居关系精确判断。

## 4. Minimax 搜索

### 4.1 Alpha-Beta 剪枝

标准 minimax 框架，最大化层（AI 回合）和最小化层（对手回合）交替递归。Alpha-Beta 剪枝在走法排序良好的情况下可将搜索节点减少 80% 以上。

终止条件：
- 游戏结束（`E.isGameOver()`）
- 深度耗尽（`depth <= 0`）
- 超时（每 1000 节点检查一次，上限 5 秒）

### 4.2 走法排序

遍历前对走法进行启发式排序，最大化剪枝率：

| 优先级 | 类型 | 分数 | 检测方式 |
|--------|------|------|---------|
| 最高 | 吃子 (remove) | 1000 | `m.remove !== null` |
| 次高 | 成行 | 500 | 模拟落子 + `E.isInMill` |
| 普通 | 放置 (place) | 10 | `m.type === 'place'` |

成行检测通过临时修改棋盘数组实现，不调用 `makeMove`，开销极小。

### 4.3 动态深度调整

当未指定深度时，根据局势动态调整搜索深度：

- **放置阶段手上子多**（≥6）：深度降至 2（分支因子高）
- **压制模式**：深度 +1（分支因子小，可多算一层）
- **决战 + 飞行**：深度 −1（分支因子爆炸，需控制）

### 4.4 时间管理

- 最大思考时间：5 秒
- minimax 每 1000 个节点检查一次
- 根节点每次迭代开头检查一次
- 超时产生的不完整评估结果被丢弃

实测数据：depth 4 通常 9-588ms，depth 5 大多 63-513ms，偶发尖峰可触及 5 秒上限。超时作为安全网保留，确保极端局面下不会无限搜索。

## 5. 静态评估函数

### 5.1 基础权重

| 因子 | 权重 | 说明 |
|------|------|------|
| material | 150 | 每多一子的分值 |
| mobility | 2 | 安全移动数 |
| threat | 15 | 潜在磨坊（差一步成行） |
| fork | 30 | 双重威胁（基础值，飞行阶段动态调整） |
| mill | 40 | 已形成的磨坊 |
| nearMill | 20 | 差一步成行（己方加成） |
| opponentNearMill | −30 | 差一步成行（对手惩罚） |

### 5.2 阶段感知权重平滑

权重随游戏阶段平滑过渡，避免放置阶段和移动阶段之间的评估断层：

- `phaseFactor = 1 − max(双方手牌数) / 9`（0 = 放置早期，1 = 手牌耗尽）
- `materialW = 150 × (1 − phaseFactor × 0.3)` — 后期材料权重降低 30%
- `mobilityW = 2 × (1 + phaseFactor × 2)` — 后期机动性权重提升 3 倍

### 5.3 飞行阶段叉子权重

飞行阶段（≤3 子，手上 0 子）时，叉子失去战术意义（对手可飞走），但多子方需要强化叉子来围杀飞行方。

检测逻辑（必须同时检查 `piecesOnBoard` 和 `piecesOnHand`，避免放置阶段误判）：

```javascript
const aiFlying = ai.piecesOnBoard <= 3 && ai.piecesOnHand === 0;
const oppFlying = opponent.piecesOnBoard <= 3 && opponent.piecesOnHand === 0;
```

权重矩阵：

| 场景 | AI fork | 对手 fork |
|------|---------|-----------|
| 双方 >3 子 | 30 | 30 |
| 双方都在飞 | 0 | 0 |
| AI 多子，对手在飞 | **60** | 0 |
| AI 在飞，对手多子 | 0 | **60** |

### 5.4 其他因子

- **飞行威胁**：对手进入飞行阶段 +50 惩罚，己方进入 −50
- **绝望修正**：材料劣势 ≥ 3 时 +20（鼓励冒险寻找翻盘机会）

## 6. 策略状态机

AI 根据局势动态切换博弈性格：

| 模式 | 触发条件 | 行为逻辑 | 策略标签 |
|------|---------|---------|---------|
| **扩张布局** | 默认 / 放置阶段 | 占据高连通性枢纽，构建潜在连线网络 | `HUB_CONTROL`, `LAYOUT` |
| **窒息压制** | 移动阶段，机动性差 > 2 | 封堵对手逃逸路径，阻止进入飞行阶段 | `SQUEEZE`, `ANTI_FLYING` |
| **决战收割** | 材料劣势 / 对手接近飞行 / 飞行阶段 | 最高搜索深度，通过双重磨坊寻求最快胜径 | `ATTACK`, `DECISIVE_STRIKE` |

策略模式影响两个层面：
1. **深度调整**：压制模式 +1 层，决战飞行 −1 层
2. **走法加成**：`applyModeBonus` 根据模式和标签对走法分数进行加减

## 7. 走法选择机制

### 7.1 基于排名的指数分布

传统的基于分数的 softmax 在分数差距大时失效（成行吃子 +240 vs 普通走法 +40，次优走法概率趋近 0）。解决方案是完全剥离分数绝对值，只基于排名：

```javascript
function pickWithWeightedRandom(sorted, temperature, topK) {
    const candidates = sorted.slice(0, Math.min(topK, sorted.length));
    const weights = candidates.map((_, i) => Math.exp(-i / temperature));
    // 轮盘赌选择
}
```

- **Top-k 截断**：只在前 k 个最优走法中选择，排除灾难性走法
- **排名权重**：`exp(-i / temperature)`，与分数绝对值无关
- **同分打散**：排序时对同分走法加入随机扰动，防止稳定排序的排名歧视

### 7.2 渐进式确定性

温度随游戏阶段变化，平衡探索与利用：

| 模式 | PLACEMENT | MOVING | FLYING | Top-k |
|------|-----------|--------|--------|-------|
| Eco | 0.8 | 0.8 | 0.8 | 2 |
| Normal | 0.3 | 0.3 | 0.3 | 3 |
| Master | 0.25 | 0.02 | 0.00 | 2 |

Master 模式的设计意图：
- **放置阶段 0.25**：开局多样性，避免镜像对局的"回音壁效应"（两个相同 AI 永远走出相同棋谱）
- **移动阶段 0.02**：接近确定性，中残局发挥完整算力
- **飞行阶段 0.00**：完全确定性，飞行阶段的随机失误代价极高

### 7.3 上下文隔离

双 AI 对战时，`selectBestMoveForPlayer` 入口处将全局配置快照到局部变量，防止温度/topK 在玩家间串线：

```javascript
const config = { ...currentConfig };
```

## 8. 陷阱检测

识别"隐藏陷阱"：在浅层搜索中看似劣势、但深层搜索中实际领先的走法。

- 浅层评估：D=0（静态评估，近零开销）
- 深层评估：D=当前配置深度（或自定义 `deepDepth`）
- `trapScore = 深层分数 − 浅层分数`
- 若 `trapScore > 阈值（默认 50）` 且 `浅层分数 < 0`，标记为 `HIDDEN_TRAP`

用途：在特定模式下，AI 可以故意选择看似"失误"的走法诱导对手犯错。

## 9. 性能模式

| 模式 | 深度 | 陷阱检测 | 温度 | Top-k | 用途 |
|------|------|---------|------|-------|------|
| Eco | 1 | 关 | 0.8 | 2 | 轻量级，模拟新手 |
| Normal | 3 | 开 | 0.3 | 3 | 均衡，适合普通玩家 |
| Master | 4 | 开 | 阶段映射 | 2 | 全力，竞技级 |

## 10. 兜底安全

多层兜底确保主决策接口永不返回 null：

1. `moves.length === 0` → 返回 null（游戏应已结束）
2. 超时导致 `moveScores` 为空 → 返回第一个合法走法
3. `pickWithWeightedRandom` 返回 null → 返回第一个合法走法

## 11. 测试基础设施

- `test/battle.js`：双 AI 对战运行器，逐手记录棋盘状态、分数、标签、策略、耗时
- `test/run_battles.sh`：24 场循环赛（3 级模式 × 所有配对 × 4 轮轮流先手）
- 典型结果：Master > Normal > Eco，正常对局无平局，局数 20-90 手

## 12. 设计亮点

以下是在开发过程中发现并解决的关键问题，对同类博弈 AI 项目有参考价值：

### 12.1 半回合水平线效应

**问题**：九子棋的"成行+吃子"是两个 ply，但只算一个逻辑回合。如果搜索深度在成行时耗尽，AI 看到对手成行（−40）却看不到吃子（−150），导致不会主动防守。

**解决**：成行时不扣减深度，确保吃子 ply 被搜索到。

### 12.2 排名指数分布

**问题**：基于分数的 softmax 在大分差下退化为 argmax，随机性失效。

**解决**：改用基于排名的指数分布 + Top-k 截断。概率只取决于排名，与分数绝对值无关。

### 12.3 渐进式确定性

**问题**：零温度的镜像对局产生"回音壁效应"，先手永远被后手绞杀。

**解决**：Master 模式在放置阶段保留 0.25 温度增加开局多样性，后期收敛到近确定性。

### 12.4 非对称飞行阶段评估

**问题**：飞行阶段叉子失去意义（对手可飞走），但一刀切清零会导致多子方放弃叉子围杀。

**解决**：分别检测双方是否在飞行阶段，飞行方 fork=0，多子方 fork=60（强化围杀）。

### 12.5 走法排序与成行嗅探

**问题**：Alpha-Beta 剪枝效率高度依赖走法遍历顺序。

**解决**：吃子 > 成行 > 放置的优先级排序。成行检测通过临时修改棋盘数组实现，不调用 `makeMove`，开销极小。
