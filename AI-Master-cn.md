# 九连棋 AI 引擎设计规范

## 1. 概述

基于 Minimax + Alpha-Beta 搜索构建的竞技级九连棋 AI，由动态策略状态机和阶段感知评估函数驱动。支持三种性能模式（Eco / Normal / Master），采用渐进式确定性机制，并生成结构化战术报告以对接 LLM 自然语言输出。

## 2. 系统架构

采用"感知-决策-表达"三层分离架构：

1. **引擎层 (engine.js)** — 棋盘状态维护、`makeMove` / `undoMove`、合法走法生成、FEN 序列化。`undoMove` 接口是 AI 深度搜索的基石。
2. **战术分析层 (strategy.js)** — 将原始棋盘坐标转化为结构化数据：机动性扫描、阵型张力评估、战术报告生成。
3. **AI 控制层 (ai.js)** — Minimax 搜索、静态评估、策略状态机、性能模式管理。在线模式下，战术报告喂给 LLM 生成自然语言对白；离线模式下，本地状态机直接选择走法。

## 3. Minimax 搜索

### 3.1 核心：Alpha-Beta 剪枝

标准 minimax + alpha-beta 剪枝。终止条件：游戏结束、深度耗尽（`depth <= 0`）、或超时。

### 3.2 半回合水平线效应修复

九子棋中，"成行"和"吃子"是两个独立的 ply（引擎状态：`formedMill → millMove → remove`）。如果每步都无条件 `depth - 1`，搜索会在深度边界处漏掉对手的吃子动作——AI 看到了成行（−40 惩罚），却看不到紧随其后的吃子（−150）。

**修复**：当 `formedMill` 为 true 时，当前玩家的逻辑回合尚未结束（吃子待执行），不扣减深度：

```javascript
const nextDepth = result.formedMill ? depth : depth - 1;
```

确保"成行 + 吃子"只消耗 1 层搜索深度，与逻辑回合结构一致。

### 3.3 走法排序提升剪枝效率

遍历前对走法进行轻量启发式排序，最大化 alpha-beta 剪枝率：

- **吃子 (remove)**：优先级 1000
- **成行**：优先级 500（通过轻量棋盘模拟 + `E.isInMill` 检测）
- **放置 (place)**：优先级 10

成行检测临时设置 `board[to] = player` 后调用 `isInMill`，不触发 `makeMove`，开销极小。

### 3.4 时间管理

- 最大思考时间：5 秒
- minimax 每 1000 个节点检查一次
- 根节点每次迭代开头检查一次
- 超时产生的不完整评估结果被丢弃

## 4. 静态评估

### 4.1 基础权重

| 因子 | 权重 | 说明 |
|------|------|------|
| material | 150 | 每多一子的分值 |
| mobility | 2 | 安全移动数 |
| threat | 15 | 潜在磨坊 (NEAR_MILL) |
| fork | 30 | 双重威胁（基础值，飞行阶段动态调整） |
| mill | 40 | 已形成的磨坊 |
| nearMill | 20 | 差一步成行（己方） |
| opponentNearMill | −30 | 差一步成行（对手） |

### 4.2 阶段感知权重平滑

材料和机动性权重通过 `phaseFactor`（0 = 放置早期，1 = 移动/飞行阶段）平滑过渡：

- `materialW = 150 * (1 − phaseFactor * 0.3)` — 后期材料权重降低 30%
- `mobilityW = 2 * (1 + phaseFactor * 2)` — 后期机动性权重提升 3 倍

### 4.3 非对称飞行阶段叉子权重

当某方进入飞行阶段（盘面 ≤3 子，手上 0 子）时，几何上的"叉子"结构失去战术意义，因为对手可以飞走。但如果只有一方在飞，多子方应**强化**叉子权重来驱动叉子围杀。

检测（必须同时检查 `piecesOnBoard` 和 `piecesOnHand`，避免放置阶段误判）：

```javascript
const aiFlying = ai.piecesOnBoard <= 3 && ai.piecesOnHand === 0;
const oppFlying = opponent.piecesOnBoard <= 3 && opponent.piecesOnHand === 0;
```

权重矩阵：

| 场景 | AI fork 权重 | 对手 fork 权重 |
|------|-------------|---------------|
| 双方 >3 子 | 30 | 30 |
| 双方都在飞（≤3） | 0 | 0 |
| AI >3，对手在飞 | **60** | 0 |
| AI 在飞，对手 >3 | 0 | **60** |

多子方获得双倍 fork 权重，积极寻求叉子围杀飞行对手。

### 4.4 其他评估因子

- **飞行威胁**：对手/AI 进入飞行阶段时 ±50 惩罚
- **绝望修正**：材料劣势 ≥ 3 时 +20（鼓励冒险）

## 5. 走法选择：基于排名的指数分布

### 5.1 基于分数的 Softmax 的问题

朴素的 softmax 在分数差距大时彻底失效（例如成行吃子 +240 vs 普通走法 +40）。即使温度设到 3.0，次优走法的概率仍趋近于 0，随机性名存实亡。

### 5.2 解决方案：Top-k + 排名指数分布

```javascript
function pickWithWeightedRandom(sorted, temperature, topK) {
    const candidates = sorted.slice(0, Math.min(topK, sorted.length));
    const weights = candidates.map((_, i) => Math.exp(-i / temperature));
    // 轮盘赌选择
}
```

- **Top-k 截断**（默认 3）：将灾难性走法排除在候选池之外
- **排名权重**：`exp(-i / temperature)` — 完全独立于分数绝对值
- **同分打散**：排序比较器中对同分走法加入 `Math.random() - 0.5` 随机扰动，防止稳定排序导致的排名歧视

### 5.3 渐进式确定性

温度随游戏阶段变化，平衡探索与利用：

| 模式 | PLACEMENT | MOVING | FLYING | Top-k |
|------|-----------|--------|--------|-------|
| Eco | 0.8 | 0.8 | 0.8 | 2 |
| Normal | 0.3 | 0.3 | 0.3 | 3 |
| Master | 0.25 | 0.02 | 0.00 | 2 |

- **Eco**：全程高随机，模拟人类直觉和偶尔失误
- **Normal**：全程中等随机
- **Master**：开局多样性（0.25）避免镜像对局的回音壁效应，中局接近确定性（0.02），飞行阶段完全确定性（0.00）

温度解析支持标量（固定值）和对象（阶段映射）两种格式，通过 `resolveTemperature` 统一处理。

## 6. 策略状态机

三种模式，由局势动态触发：

| 模式 | 触发条件 | 行为逻辑 |
|------|---------|---------|
| **扩张布局 (Expansion)** | 默认 / 放置阶段 | 占据高连通性枢纽，构建潜在连线网络 |
| **窒息压制 (Suppression)** | 移动阶段，机动性占优 | 封堵对手逃逸路径，阻止进入飞行阶段 |
| **决战收割 (Decisive)** | 材料劣势、对手接近飞行、或飞行阶段 | 最高搜索深度，通过双重磨坊寻求最快胜径 |

模式判定使用 `report.context.phase`、`materialDiff`、`isOpponentNearFlying`、`metrics.mobilityGap`。

## 7. 陷阱检测（深度差评估）

识别"隐藏陷阱"：在浅层搜索中看似劣势、但深层搜索中实际领先的走法。

- 浅层评估：D=0（静态评估，近零开销）
- 深层评估：D=浅层+2（或自定义 `deepDepth`）
- 若 `trapScore = 深层 − 浅层 > 阈值` 且 `浅层 < 0`，标记为 `HIDDEN_TRAP`

## 8. 性能模式

| 模式 | 深度 | 陷阱检测 | 温度 | Top-k | 标签 |
|------|------|---------|------|-------|------|
| Eco | 1 | 关 | 0.8 | 2 | 轻量 |
| Normal | 3 | 开 | 0.3 | 3 | 均衡 |
| Master | 4 | 开 | 阶段映射 | 2 | 全力 |

动态深度调整（未指定深度时生效）：
- 放置阶段手上子多：深度上限 2
- 压制模式：深度 +1（上限 6）
- 决战 + 飞行：深度 −1（下限 2）

## 9. 上下文隔离

双 AI 对战时，`selectBestMoveForPlayer` 入口处将全局配置快照到局部变量，防止温度/topK 在玩家间串线：

```javascript
const config = { ...currentConfig };
```

所有下游引用（深度、温度、topK、标签）均使用此局部快照。

## 10. 兜底安全

多层兜底确保主决策接口永不返回 null：

1. `moves.length === 0` → 返回 null（无合法走法，游戏应已结束）
2. 超时导致 `moveScores` 为空 → 返回第一个合法走法，分数 0
3. `pickWithWeightedRandom` 返回 null → 返回第一个合法走法，分数 0

## 11. 测试基础设施

- `test/battle.js`：双 AI 对战运行器，逐手记录（棋盘状态、分数、标签、策略、耗时）
- `test/run_battles.sh`：24 场循环赛（3 级模式 × 所有配对 × 4 轮轮流先手）
- 典型结果：Master > Normal > Eco，正常对局无平局，局数 20-90 手

## 12. 设计决策记录

| 决策 | 理由 |
|------|------|
| formedMill 深度修复 | 防止水平线效应——AI 在深度边界漏看对手吃子 |
| 排名指数分布 | 消除 softmax 中分数绝对值导致的概率坍缩 |
| 渐进式确定性 | 开局多样性防止回音壁；后期确定性防止失误 |
| 非对称 fork 权重 | 多子方需要强化 fork 围杀飞行对手；飞行方 fork 无意义 |
| Top-k 截断 | 防止灾难性走法进入随机选择池 |
| 同分打散 | 防止稳定排序对同分走法产生不公平的排名歧视 |
