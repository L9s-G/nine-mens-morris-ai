
### 第一步：Engine.js — 逻辑闭环建议

你的撤销逻辑表格已经非常完善，但有一个**隐藏状态**需要注意：

-   **`millMove` 的推导逻辑**：
    
    在 `type === 'remove'` 时恢复 `millMove = true` 是正确的，因为这代表该玩家刚刚成行并执行了吃子。但在撤销这一步之后，如果你还要继续撤销“成行”的那一步（`place` 或 `move`），记得要把 `millMove` 归零。
    
-   **`piecesLost` 的处理**：
    
    在你的 `moveHistory` 里记录 `remove`（被吃棋子的索引）时，最好顺便记录这个棋子是谁的（虽然可以推导，但记录下来更稳健）。
    

----------

### 第二步：Strategy.js — 战术分析的“杀手锏”

在实现“有效机动性”时，有一个算法小技巧：

-   **不要只看“走后即死”**：
    
    真正的宗师 AI 会看“走后对方是否迫使我必须走某一步”。
    
-   **标签（Tags）的颗粒度**：
    
    建议在 `suggestedMoves` 中加入一个 `risk` 字段。这能让第四步的 LLM 更有戏：
    
    -   `risk: "high"` → LLM 话术：“这是一场豪赌，你敢接吗？”
        
    -   `risk: "low"` → LLM 话术：“稳扎稳打，你毫无机会。”
        

----------

### 第三步：ai.js — 搜索深度的艺术

关于 **D=2 到 D=8** 的深度差：

-   **D=2 (直觉层)**：模拟人类新手的视觉直觉（只看一步成行）。
    
-   **D=8 (深谋远虑层)**：这是算法的真正强度。
    
-   **HiddenTrap 的判断**：
    
    $$TrapScore = Score(D_8) - Score(D_2)$$
    
    如果 $TrapScore$ 很高，说明这步棋表面看起来很烂（D2 评分低），但实际是神来之笔（D8 评分高）。
    

----------

### 第四步：Narrator.js — 语义衔接

在设计词库时，可以考虑“情绪值”：

-   根据 `materialDiff`（子数差）的变化，让离线 AI 的对白从“狂妄”转为“气馁”。
    

----------

### 架构示意图 (Architecture Overview)

为了让你在编码前对全局数据流有直观感受，可以参考这个逻辑结构：

### 你的下一步

这个计划已经具备了量产级的工程水准。**建议现在就开始第一步：`Engine.js` 的 `undoMove` 实现。**

当你写完这个函数时，你可以做一个简单的压力测试：

1.  随机执行 100 步 `makeMove`（记录每一步）。
    
2.  执行 100 步 `undoMove`。
    
3.  检查最终的 `board` 和 `state` 是否与初始状态**完全一致**。
    
