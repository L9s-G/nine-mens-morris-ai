# Nine Men's Morris

纯前端九子棋游戏，内置具有人格的 AI 对手。

**在线体验**：浏览器打开 `index.html`，零依赖，无需安装。

## 游戏规则

九子棋是两人对弈的古老策略棋盘游戏，棋盘由 24 个交点和连接线组成。

**流程**：

1. **放置阶段**（PLACEMENT）：双方各 9 子，轮流放到棋盘空位
2. **走子阶段**（MOVING）：手上的棋子放完后，沿连线移动到相邻空位
3. **飞行阶段**（FLYING）：场上仅剩 3 子时，可飞行到任意空位

**核心机制**：

- **成磨坊**（Mill）：三子连成一线（横/竖），即"成行"
- **吃子**：成行后可吃掉对方一颗棋子（优先吃不在磨坊中的棋子）
- **胜利条件**：将对手棋子减少到 2 颗，或堵死对手所有走法

## 功能特性

- **3 级 AI 难度**：菜鸟 / 老手 / 大师
- **3 套视觉主题**：暗夜、马卡龙、赛博
- **先手选择**：玩家先手或 AI 先手
- **AI 人格系统**：根据局面情绪说出台词（弹幕/气泡两种呈现）
- **棋谱导出**：游戏结束后可复制完整对战记录
- **Debug 模式**：双击棋盘中心显示位置编号和 AI 思考细节
- **响应式设计**：竖屏弹幕、横屏气泡聊天
- **三次重复判和**：同一局面出现三次自动判平局
- **PWA 支持**：可离线使用

## 项目结构

```
├── index.html          # 入口（SVG 棋盘 + 状态面板 + 模态框）
├── style.css           # 样式（CSS 变量 + 3 套主题 + 响应式布局）
├── engine.js           # 核心引擎（棋盘拓扑、规则、走法、FEN）
├── evaluator.js        # 局面评估（磨坊分析、机动性、静态评分）
├── searcher.js         # 搜索引擎（Minimax + αβ + 迭代加深 + 时间墙）
├── ai.js               # AI 控制器（难度配置、温度随机选法）
├── narrator.js         # AI 人格（标签体系 + 情绪台词 + 弹幕/气泡）
├── game.js             # UI 控制（渲染、交互、动画、设置、存档）
├── service-worker.js   # PWA 离线缓存
├── manifest.json       # PWA 清单
└── test/
    ├── battle.js       # AI 对战脚本（Node.js）
    ├── run_battles.sh  # 循环赛脚本
    └── analyze.py      # 对战日志分析
```

## 架构设计

所有模块为 IIFE 全局单例，通过 `<script>` 标签加载，依赖方向单一：

```
┌─────────────────────────────────────────────┐
│  game.js (UI 控制)                          │
│  棋盘渲染 · 交互动画 · 设置管理 · 存档      │
├──────────────┬──────────────────────────────┤
│   ai.js      │       narrator.js            │
│  难度配置    │  三层标签 · 情绪台词          │
│  温度选法    │  弹幕/气泡呈现                │
├──────┬───────┴──────────────────────────────┤
│ searcher.js  │  evaluator.js                │
│ Minimax+αβ   │  磨坊威胁 · 机动性           │
│ 迭代加深     │  静态评分 · 深度加权          │
│ 5s 时间墙    │                              │
├──────┴──────────────────────────────────────┤
│  engine.js (核心引擎)                        │
│  24 位拓扑 · 走法生成/执行/撤销 · FEN       │
│  16 条磨坊线 · 三次重复检测 · 终局判定      │
└─────────────────────────────────────────────┘
```

## AI 引擎

### 搜索算法

- **Minimax + Alpha-Beta 剪枝**：标准博弈树搜索
- **迭代加深**：深度 1 逐步加深到目标层，每层走法排序复用于下一层
- **时间墙**：默认 5 秒，每 1024 节点检查一次，超时返回当前最优
- **成磨不消耗深度**：形成磨坊后同一玩家继续吃子，不计入搜索深度

### 难度配置

| 难度 | 搜索深度 | 温度 | Top-K | 典型用时 |
|------|---------|------|-------|---------|
| 菜鸟 | 1-2     | 1.0  | 5     | ~2ms    |
| 老手 | 2-3     | 0.8  | 4     | ~34ms   |
| 大师 | 3-4     | 0~0.25 | 2  | ~319ms  |

温度越高走法越随机，Top-K 越大候选越多。大师模式在不同阶段使用不同温度。

### 评估体系（evaluator.js）

- 终局分数（±10000），深度加权偏好更快的胜利
- 磨坊威胁：nearMill、hardNearMill、rollingFork、hardRollingFork
- 机动性：半衰递减 `0.5^(mobility-1)`
- 吃子价值：非线性，取决于对手剩余子数

## AI 人格系统

narrator.js 通过三层标签体系生成个性化台词：

- **Layer 1 位置标签**：走前评估，描述局面特征（PHASE_PLACEMENT / PHASE_FLYING 等）
- **Layer 2 走法标签**：走后评估，描述走法性质（MILL / CAPTURE / BLOCK 等）
- **Layer 3 合成标签**：两阶段合并推导的高层语义

**情绪分级**（基于 minimax 评分）：

| 情绪 | 分数范围 | 语气 |
|------|---------|------|
| 傲慢 | >= 500  | 自信、嘲讽 |
| 从容 | 100~499 | 淡定、暗示 |
| 中性 | -99~99  | 平实 |
| 谨慎 | -100~-399 | 反击、挑衅 |
| 绝望 | < -400  | 自嘲、认怂 |

台词匹配优先级：多标签组合 > 单标签 > 模式回退。

## 测试工具

```bash
# 单场对战
node test/battle.js Normal Master 1 output.log

# 24 场循环赛（3 级 AI 全组合）
bash test/run_battles.sh

# 对战日志分析
python3 test/analyze.py [logs_dir]
```

## 技术细节

- **棋盘表示**：24 位数组，`null` / `1`（白/玩家）/ `2`（黑/AI）
- **棋盘拓扑**：预计算邻居表 + 16 条磨坊线
- **FEN 序列化**：`board/currentPlayer/white/black/millMove`
- **位置哈希**：3 进制增量哈希，滑动窗口（36 步）检测三次重复
- **SVG 棋盘**：viewBox 600x600，24 交点通过 7x7 网格坐标映射

## 快速开始

```bash
git clone <repo-url>
cd nine-mens-morris-ai
start index.html    # Windows
open index.html     # macOS
xdg-open index.html # Linux
```

零依赖，无构建步骤。
